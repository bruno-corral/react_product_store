import { MainRoutes } from "../routes/MainRoutes"

export const Main = () => {
    return (
        <div className="container mx-auto flex items-center flex-1">
            <MainRoutes />
        </div>
    );
}